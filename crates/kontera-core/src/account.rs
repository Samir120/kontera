// crates/kontera-core/src/account.rs
//! `AccountNumber` and `AccountKind` — BAS chart primitives. See docs/02 §2.2.
//!
//! An `AccountNumber` is parsed at the boundary, so `class()` is infallible.
