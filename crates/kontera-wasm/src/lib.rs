//! `wasm-bindgen` bindings for kontera, published to npm rather than crates.io.
//!
//! Empty until M5. The crate exist now so the workspace shape is complete and
//! CI compiles every member.

#![forbid(unsafe_code)]
#![warn(missing_docs)]
