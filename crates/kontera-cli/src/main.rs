//! Command-line interface for kontera.
//!
//! `reply`and `validate` are built in M4. This stub exist so the workspace
//! builds and so `cargo run -p kontera-cli` fails loudly rather than silently

#![forbid(unsafe_code)]

fn main() {
    eprintln!("kontera: not implemented yet - the CLI is built in M4.");
    eprintln!("See docs/06-roadmap.md.");
    std::process::exit(2);
}
